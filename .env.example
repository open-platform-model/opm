# OPM Taskfile Environment Configuration
# Copy this file to .env and customize as needed

# CUE Configuration
# -----------------

# Override CUE cache directory (auto-detected if not set)
# Default: $XDG_CACHE_HOME/cue or $HOME/.cache/cue
# CUE_CACHE_DIR=/custom/path/to/cue/cache

# Registry to use for CUE module operations
# Default: localhost:5000 for local development
# CUE_REGISTRY=localhost:5000

# Docker Registry Configuration
# -----------------------------

# Local registry container name
# Default: opm-registry
# REGISTRY_CONTAINER=opm-registry

# Local registry port
# Default: 5000
# REGISTRY_PORT=5000

# Local registry data directory (relative to opm/)
# Default: .registry-data
# REGISTRY_DATA_DIR=.registry-data

# Module Configuration
# --------------------

# Module path for publishing
# Default: opm.dev@v1
# MODULE_PATH=opm.dev@v1

# Development version for local testing
# VERSION=v0.1.0-dev

# Task Configuration
# ------------------

# NOTE: Tasks run in silent mode by default (commands hidden, only output shown)
# To show commands being executed, use: task --verbose <taskname> or task -v <taskname>
# There is no environment variable to control this - use the CLI flag instead
