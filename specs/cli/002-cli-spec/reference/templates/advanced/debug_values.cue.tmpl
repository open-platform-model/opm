// Debug values provide comprehensive configuration for validation.
// Use with: opm mod vet --concrete
// This file explicitly sets all values to enable full schema validation.
package main

// Override values with complete concrete values for debugging
values: {
	web: {
		image:    "nginx:1.25"
		replicas: 2
		port:     8080
	}

	api: {
		image:    "node:20-alpine"
		replicas: 3
		port:     3000
	}

	worker: {
		image:    "python:3.12-alpine"
		replicas: 2
	}

	db: {
		image:       "postgres:16-alpine"
		storageSize: "10Gi"
		port:        5432
	}
}
